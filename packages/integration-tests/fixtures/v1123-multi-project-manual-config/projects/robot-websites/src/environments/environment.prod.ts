export const environment = {
  production: true,
  apiUrl: 'https://my-api-production.com', // replace with your production API URL
  firebaseConfig: {
    apiKey: 'your-production-api-key', // replace with your production Firebase API key
    authDomain: 'your-production-auth-domain', // replace with your production Firebase auth domain
    databaseURL: 'your-production-database-url', // replace with your production Firebase database URL
    projectId: 'your-production-project-id', // replace with your production Firebase project ID
    storageBucket: 'your-production-storage-bucket', // replace with your production Firebase storage bucket
    messagingSenderId: 'your-production-messaging-sender-id', // replace with your production Firebase messaging sender ID
    appId: 'your-production-app-id' // replace with your production Firebase app ID
  }
};
